# etcd cluster service endpoints, automatically generated from etcd members.
TMP_ENDPOINTS: "{% for h in groups['etcd'] %}https://{{ h }}:2379,{% endfor %}"
ETCD_ENDPOINTS: "{{ TMP_ENDPOINTS.rstrip(',') }}"

# calico AS number
CALICO_AS_NUMBER: 64512
