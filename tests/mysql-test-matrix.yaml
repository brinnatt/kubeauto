meta:
  version: 1
  updated: "2026-08-19"
  component: percona-pxc
  scope: "Independent MySQL middleware delivery branch; does not replace the enterprise core matrix"
  runner: bash tests/run_enterprise_regression.sh --mysql-only
  result: PENDING

versions:
  operator: "1.20.0"
  pxc: "8.4.8-8.1"
  xtrabackup: "8.4.0-5.1"
  haproxy: "2.8.18-1"
  fluentbit: "5.0.6-1"
  kubernetes: "1.33.6"

cases:
  - {id: MYSQL-01, group: static, desc: "Version, Chart SHA256, image and six-repository contracts", status: pending, evidence: ""}
  - {id: MYSQL-02, group: preflight, desc: "Clean namespace, node capacity, failure domains and StorageClass R/W", status: pass, evidence: "2026-08-20 clean baseline; three failure domains; three 20Gi PVCs completed write/read verification"}
  - {id: MYSQL-03, group: supply-chain, desc: "All pinned images materialized in the local Registry with manifest evidence", status: pending, evidence: ""}
  - {id: MYSQL-04, group: install, desc: "Operator, CRD, PXC 3/3, HAProxy 3/3 and PVC readiness", status: pending, evidence: ""}
  - {id: MYSQL-05, group: sql, desc: "TLS connection, Primary write, replicas read and least-privilege negative checks", status: pass, evidence: "2026-08-20 VERIFY_CA success; wrong-CA failure; Primary/Synced/Ready on all members; least-privilege DML success and DDL/GRANT refusal"}
  - {id: MYSQL-06, group: quorum, desc: "Single PXC loss keeps Primary; two-node loss refuses unsafe writes", status: pending, evidence: ""}
  - {id: MYSQL-07, group: transfer, desc: "Pod reconstruction returns through IST/SST to Primary and Synced", status: pass, evidence: "2026-08-20 old Pod UID disappeared; new UID became Ready; IST/SST evidence observed; cluster recovered 3/3"}
  - {id: MYSQL-08, group: secrets, desc: "System password rotation converges; old credential fails and new credential succeeds", status: pass, evidence: "2026-08-20 rotated root Secret; new credential succeeded and old credential failed"}
  - {id: MYSQL-09, group: backup, desc: "Full backup reaches Succeeded and the object is readable", status: pending, evidence: ""}
  - {id: MYSQL-10, group: restore, desc: "Full restore preserves the expected marker and creates a new backup baseline", status: pending, evidence: ""}
  - {id: MYSQL-11, group: pitr, desc: "PITR positive target and binlog-gap negative refusal", status: pending, evidence: ""}
  - {id: MYSQL-12, group: performance, desc: "Fixed sysbench baseline, concurrency ladder and single-node-failure measurements", status: pending, evidence: ""}
  - {id: MYSQL-13, group: idempotence, desc: "Second install causes no unintended credential, PVC or database rollout change", status: pending, evidence: ""}
  - {id: MYSQL-14, group: cleanup, desc: "Scoped cleanup removes only test resources and emits MYSQL_CLEAN_VERIFY_PASS", status: pending, evidence: ""}
