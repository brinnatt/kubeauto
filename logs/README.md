# Runtime logs

This directory is for transient local regression output. Raw `.log`, `.out`, and `.pid` files are intentionally ignored and must not be used as the source of truth for delivery status.

Promote reusable findings into `tests/`, `tests/enterprise-test-matrix.yaml`, or maintained documentation. Remove raw logs after the investigation or sign-off evidence has been captured.

